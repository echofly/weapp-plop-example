{{#if needComputed}}
import { behavior as computedBehavior } from 'miniprogram-computed'
{{/if}}
const app = getApp()

Component({
  {{#if needComputed}}
  behaviors: [computedBehavior],
  {{/if}}
  data: {},
  methods: {
    onLoad() {},
    onReady() {},
  }
})